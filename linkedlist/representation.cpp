#include <bits/stdc++.h>
using namespace std;

// singly linked list

struct Node
{
    int data;
    Node *next;
};
// double linked list

struct node
{
    int data;
    node *prev;
    node *next;
};

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    return 0;
}