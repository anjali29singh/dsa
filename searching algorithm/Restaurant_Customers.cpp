#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    vector<vector<int, int>> arr;
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < 2; j++)
            cin >> arr[i][j];
    }
    return 0;
}